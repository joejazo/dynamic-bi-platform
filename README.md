# Dynamic BI Platform

A dynamic Business Intelligence platform that enables users to:
- Upload datasets (CSV to start)
- Interact with their data and build visualizations in real time
- Chat and issue commands to the system for analytics and visualization

## Getting Started

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Navigate to http://localhost:8000/docs for the interactive API documentation.